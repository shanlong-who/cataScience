# Presence of this file stops Shiny from auto-sourcing the R/ directory in
# alphabetical order. app.R sources these files explicitly in the order the
# app needs (data and modules before the UI that uses them).
